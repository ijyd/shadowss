language: go
go:
  - 1.8
  - tip

go_import_path: shadowss


addons:
  apt:
    packages:
    - libpcap-dev
    - libaspell-dev
    - libhunspell-dev

script:
  - cd cmd/shadowss/ && go build -a  -o shadowss
